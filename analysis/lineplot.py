

class LinePlot:

    def __init__(self, settings: IDMFConfig):
        pass

    def __call__(self, xvalues: List[float], concvalues: List[float], line: LineSetting, outputpath: str):
        plot = self.plot(xvalues, concvalues, line)
        
        plt.savefig(fdjksalfdkl;afd)
